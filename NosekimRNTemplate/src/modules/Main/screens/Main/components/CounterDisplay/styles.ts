import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  counterContainer: {
    display: 'flex',
    width: '100%',
    alignItems: 'center',
    padding: 20,
  },
  countTitle: {
    fontSize: 24,
  },
  count: {
    fontSize: 48,
  },
});

export default styles;
